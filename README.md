# Remindly v1.1

React + Vite + Firebase Authentication reminder app.

## Firebase Console setup
1. Authentication → Sign-in method → Enable Email/Password.
2. Authentication → Settings → Authorized domains → add your Vercel domain if required.

## Run
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```
